{
  "schemaVersion": 2,
  "timezone": "Asia/Seoul",
  "weekStartsOn": "Monday",
  "scope": {
    "type": "server-managed-opaque-review-session",
    "publicLabels": ["A", "B"],
    "personas": ["synthetic-a", "synthetic-b"],
    "clientOwnerOverridesAccepted": false
  },
  "tables": {
    "plans": ["plan_id", "persona_id", "title", "start_date", "end_date", "priority", "success_criterion", "estimated_minutes", "carried_from"],
    "plan_history": ["plan_id", "persona_id", "revision", "source_schema_version", "event", "change_reason", "before_values", "after_values", "changed_at"],
    "todos": ["todo_id", "persona_id", "plan_id", "text", "due_date", "priority", "tags", "estimated_minutes", "status", "deleted_at"],
    "do_records": ["do_id", "persona_id", "plan_id", "todo_id", "started_at", "ended_at", "actual_minutes", "blocked_reason", "idempotency_key"],
    "see_records": ["see_id", "persona_id", "plan_id", "review_local_date", "planned_count", "completed_count", "delayed_count", "blocked_count", "estimated_minutes", "actual_minutes", "variance_minutes", "next_adjustment", "next_plan_id"],
    "see_adjustments": ["adjustment_id", "persona_id", "source_plan_id", "target_plan_id", "adjustment_text"],
    "review_sessions": ["session_token", "persona_id", "scope_label", "created_at", "updated_at"]
  },
  "relationships": [
    "plan_history.plan_id -> plans.plan_id",
    "todos.plan_id -> plans.plan_id",
    "do_records.plan_id -> plans.plan_id",
    "do_records.todo_id -> todos.todo_id",
    "see_records.plan_id -> plans.plan_id",
    "see_adjustments.source_plan_id -> plans.plan_id",
    "see_adjustments.target_plan_id -> plans.plan_id"
  ],
  "migration": {"legacyVersion": 1, "currentVersion": 2, "importIsTransactional": true},
  "dateRules": {"dateOnly": "YYYY-MM-DD", "timezone": "Asia/Seoul", "weekStartsOn": "Monday"}
}
